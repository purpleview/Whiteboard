# Coverage-Stempel

Ein Commit je gruenem Lauf auf dem Hauptbranch, erzeugt von der Action
`coverage-stamp` im Repository `development`.

`coverage-stamp.json` ist der aktuelle Stand, `history.ndjson` die Fortschreibung.
Ausgewertet von `metrics/` in `development`.
